import { IAddressSpace } from "node-opcua-address-space-base";
import { DataType } from "node-opcua-basic-types";
import { NodeId } from "node-opcua-nodeid";
export declare function validateDataTypeCorrectness(addressSpace: IAddressSpace, dataTypeNodeId: NodeId, variantDataType: DataType, allowNulls: boolean, context?: {
    toString(): string;
}): boolean;
